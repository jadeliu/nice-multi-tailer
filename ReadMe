To run:
1. install virtualenv:
    pip install virtualenv
2. source virtualenv:
    source venv/bin/activate
3. install packages:
    pip install -r requirements.txt
4. run program:
    python run.py -D [directory] -T [maxwait] -B
    for testing, please use "./sample_logs/" as directory argument
5. generate logs for testing:
    pyhton gen_log.py

