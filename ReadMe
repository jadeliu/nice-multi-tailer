To run:
1. install virtualenv:
    pip install virtualenv
2. source virtualenv:
    source venv/bin/activate
3. install packages:
    pip install -r requirements.txt
4. run program:
    python run.py -D [directory] -T [maxwait] -B 

